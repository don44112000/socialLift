(function () {
  "use strict";

  // Client-side Facebook SDK login is disabled.
  // The frontend redirects the user directly to the backend at /auth/facebook/login
  // to complete the secure OAuth flow.
})();
